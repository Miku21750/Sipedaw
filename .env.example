DATABASE_URL="postgresql://postgres:postgres@localhost:5432/sipedaw?schema=public"
AUTH_SECRET="replace-with-at-least-32-random-characters"
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
NIK_ENCRYPTION_KEY="replace-with-a-base64-encoded-32-byte-key"
NIK_HASH_SECRET="replace-with-a-different-random-secret-at-least-32-characters"
