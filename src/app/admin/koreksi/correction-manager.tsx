"use client";import{useCallback,useEffect,useState}from"react";type Item={id:string;reason:string;proposedData:Record<string,unknown>;status:string;reviewNote:string|null;resident:{fullName:string;nikLastFour:string};requestedBy:{name:string}};export function CorrectionManager(){const[rows,setRows]=useState<Item[]>([]);const[message,setMessage]=useState("");const load=useCallback(async()=>{const r=await fetch("/api/admin/corrections");const j=await r.json();if(r.ok)setRows(j.data)},[]);useEffect(()=>{void load()},[load]);async function review(id:string,decision:"APPROVED"|"REJECTED"){const reviewNote=prompt(decision==="APPROVED"?"Catatan persetujuan (opsional)":"Alasan penolakan")||"";const r=await fetch(`/api/admin/corrections/${id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({decision,reviewNote})});const j=await r.json();setMessage(j.message);if(r.ok)await load()}return <div className="card">{message&&<div className="alert">{message}</div>}<div className="table-wrap"><table><thead><tr><th>Warga</th><th>Petugas</th><th>Perubahan</th><th>Alasan</th><th>Status</th><th>Aksi</th></tr></thead><tbody>{rows.map(r=><tr key={r.id}><td>{r.resident.fullName}<br/><span className="muted">****{r.resident.nikLastFour}</span></td><td>{r.requestedBy.name}</td><td><code>{JSON.stringify(r.proposedData)}</code></td><td>{r.reason}</td><td>{r.status}</td><td className="actions">{r.status==="PENDING"&&<><button onClick={()=>review(r.id,"APPROVED")}>Setujui</button><button className="danger-button" onClick={()=>review(r.id,"REJECTED")}>Tolak</button></>}</td></tr>)}{!rows.length&&<tr><td colSpan={6} className="muted">Belum ada pengajuan.</td></tr>}</tbody></table></div></div>}
