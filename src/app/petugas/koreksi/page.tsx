import{redirect}from"next/navigation";import{requireSession}from"@/lib/auth";import{AppHeader}from"@/components/app-header";import{CorrectionForm}from"./correction-form";export default async function CorrectionPage(){const user=await requireSession();if(user.role!=="FIELD_OFFICER")redirect("/admin");return <><AppHeader user={user}/><main className="container"><h1>Pengajuan Koreksi</h1><CorrectionForm/></main></>}
